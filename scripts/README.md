# Scripts

This folder holds manually run operational scripts and diagnostics that should not live loose at the repository root.

- `api/` for integration and API exercises
- `security/` for scanning and security helpers
- `diagnostics/` for one-off investigation helpers
