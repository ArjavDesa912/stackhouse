{{- if .Values.stackhouse.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "stackhouse.fullname" . }}-ingress
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "stackhouse.labels" . | nindent 4 }}
  {{- with .Values.stackhouse.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.stackhouse.ingress.className }}
  ingressClassName: {{ .Values.stackhouse.ingress.className }}
  {{- end }}
  {{- if .Values.stackhouse.ingress.tls }}
  tls:
    {{- range .Values.stackhouse.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.stackhouse.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "stackhouse.fullname" $ }}-api
                port:
                  number: {{ $.Values.stackhouse.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
