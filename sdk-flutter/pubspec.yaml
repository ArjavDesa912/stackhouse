name: stackhouse_flutter
description: A Flutter client for Stackhouse.
version: 0.1.0
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  http: ^1.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
